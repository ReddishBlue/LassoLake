using UnityEngine;

public class LittleGuyTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public AnimalSpawner mySpawner; //when the spawner instantiates the animal, it sets this parameter

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
